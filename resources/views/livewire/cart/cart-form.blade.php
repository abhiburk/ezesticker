<div class="form-row">
    <p>
        To Buy Call Us @ +91 7977392139
    </p>
    {{-- <div class="form-group col-md flex-grow-0">
        <div class="input-group mb-3 input-spinner">
            <div class="input-group-prepend">
                <button class="btn btn-light " type="button" id="button-plus" wire:click="decrement"> - </button>
            </div>
            <input type="text" class="form-control" wire:model="quantity">
            <div class="input-group-append">
                <button class="btn btn-light " type="button" id="button-minus" wire:click="increment"> + </button>
            </div>
        </div>
    </div> 
    <div class="form-group col-md">
        <x-utils.button wire:click.prevent="addToCart" wireTarget="addToCart" class="btn btn-outline-warning">
            <i class="fas fa-shopping-cart mr-1"></i> <span class="text">Add to cart </span> 
        </x-utils.button>

        @if (Cart::getContent()->count() > 0)
            <a href="{{ route('shop.checkout') }}" class="btn-warning btn"> Checkout</i> </a>
        @else
            <button class="btn-warning btn" type="button" disabled>Checkout</button>
        @endif
    </div> --}}
</div>